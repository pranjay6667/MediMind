
// Firebase configuration is replaced with mocks as the environment
// does not support the firebase module imports correctly.
// The application now uses LocalStorage for data persistence.

export const app = {};
export const analytics = {};
export const db = {};
export const auth = {};
